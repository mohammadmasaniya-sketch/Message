repeat task.wait() until game:IsLoaded()

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local SoundService = game:GetService("SoundService")
local Player = Players.LocalPlayer

local isMobile = UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled
local guiScale = isMobile and 0.4 or 1

local C = {
    bg        = Color3.fromRGB(0, 0, 0),
    sidebar   = Color3.fromRGB(5, 5, 12),
    blue      = Color3.fromRGB(0, 80, 255),
    blueLight = Color3.fromRGB(60, 130, 255),
    blueDark  = Color3.fromRGB(0, 50, 180),
    accent    = Color3.fromRGB(0, 80, 255),
    text      = Color3.fromRGB(255, 255, 255),
    textDim   = Color3.fromRGB(100, 160, 255),
    success   = Color3.fromRGB(34, 197, 94),
    danger    = Color3.fromRGB(239, 68, 68),
    border    = Color3.fromRGB(0, 60, 200),
    rowBg     = Color3.fromRGB(8, 8, 20),
    rowHov    = Color3.fromRGB(12, 12, 30),
    onBg      = Color3.fromRGB(0, 80, 255),
    offBg     = Color3.fromRGB(25, 25, 45),
}

-- ── State ──────────────────────────────────────────────────────────────────
local unwalkEnabled      = false
local unwalkAnimations   = {}
local autoLeftEnabled    = false
local autoRightEnabled   = false
local medusaCounterEnabled = false
local medusaConns        = {}
local medusaDebounce     = false
local medusaLastUsed     = 0
local MEDUSA_COOLDOWN    = 25

local KB = {
    AutoLeft    = { kb = Enum.KeyCode.Z,  gp = nil },
    AutoRight   = { kb = Enum.KeyCode.C,  gp = nil },
    DropBrainrot= { kb = Enum.KeyCode.X,  gp = nil },
    TPFloor     = { kb = Enum.KeyCode.F,  gp = nil },
}

local AP_L1      = Vector3.new(-476.48,-6.28, 92.73)
local AP_L2      = Vector3.new(-483.12,-4.95, 94.80)
local AP_L_FACE  = Vector3.new(-482.25,-4.96, 92.09)
local AP_R1      = Vector3.new(-476.16,-6.52, 25.62)
local AP_R2      = Vector3.new(-483.06,-5.03, 25.48)
local AP_R_FACE  = Vector3.new(-482.06,-6.93, 35.47)

local NS = 60

-- Auto-left / right connections
local alConn, arConn = nil, nil
local alPhase, arPhase = 1, 1

local function stopAutoLeft()
    if alConn then alConn:Disconnect(); alConn = nil end
    alPhase = 1
    local char = Player.Character
    if char then
        local h = char:FindFirstChildOfClass("Humanoid")
        if h then h:Move(Vector3.zero, false) end
    end
end

local function stopAutoRight()
    if arConn then arConn:Disconnect(); arConn = nil end
    arPhase = 1
    local char = Player.Character
    if char then
        local h = char:FindFirstChildOfClass("Humanoid")
        if h then h:Move(Vector3.zero, false) end
    end
end

local autoLeftSetVisual, autoRightSetVisual

local function startAutoLeft()
    if alConn then alConn:Disconnect() end
    alPhase = 1
    alConn = RunService.Heartbeat:Connect(function()
        if not autoLeftEnabled then return end
        local char = Player.Character; if not char then return end
        local hrp = char:FindFirstChild("HumanoidRootPart")
        local hum = char:FindFirstChildOfClass("Humanoid")
        if not hrp or not hum then return end
        if alPhase == 1 then
            local tgt = Vector3.new(AP_L1.X, hrp.Position.Y, AP_L1.Z)
            if (tgt - hrp.Position).Magnitude < 1 then
                alPhase = 2
                local d = AP_L2 - hrp.Position; local mv = Vector3.new(d.X,0,d.Z).Unit
                hum:Move(mv,false); hrp.AssemblyLinearVelocity = Vector3.new(mv.X*NS, hrp.AssemblyLinearVelocity.Y, mv.Z*NS); return
            end
            local d = AP_L1 - hrp.Position; local mv = Vector3.new(d.X,0,d.Z).Unit
            hum:Move(mv,false); hrp.AssemblyLinearVelocity = Vector3.new(mv.X*NS, hrp.AssemblyLinearVelocity.Y, mv.Z*NS)
        elseif alPhase == 2 then
            local tgt = Vector3.new(AP_L2.X, hrp.Position.Y, AP_L2.Z)
            if (tgt - hrp.Position).Magnitude < 1 then
                hum:Move(Vector3.zero,false); hrp.AssemblyLinearVelocity = Vector3.zero
                autoLeftEnabled = false
                if alConn then alConn:Disconnect(); alConn = nil end
                alPhase = 1
                if autoLeftSetVisual then autoLeftSetVisual(false) end
                if (AP_L_FACE - hrp.Position).Magnitude > 0.01 then
                    hrp.CFrame = CFrame.new(hrp.Position, Vector3.new(AP_L_FACE.X, hrp.Position.Y, AP_L_FACE.Z))
                end
                return
            end
            local d = AP_L2 - hrp.Position; local mv = Vector3.new(d.X,0,d.Z).Unit
            hum:Move(mv,false); hrp.AssemblyLinearVelocity = Vector3.new(mv.X*NS, hrp.AssemblyLinearVelocity.Y, mv.Z*NS)
        end
    end)
end

local function startAutoRight()
    if arConn then arConn:Disconnect() end
    arPhase = 1
    arConn = RunService.Heartbeat:Connect(function()
        if not autoRightEnabled then return end
        local char = Player.Character; if not char then return end
        local hrp = char:FindFirstChild("HumanoidRootPart")
        local hum = char:FindFirstChildOfClass("Humanoid")
        if not hrp or not hum then return end
        if arPhase == 1 then
            local tgt = Vector3.new(AP_R1.X, hrp.Position.Y, AP_R1.Z)
            if (tgt - hrp.Position).Magnitude < 1 then
                arPhase = 2
                local d = AP_R2 - hrp.Position; local mv = Vector3.new(d.X,0,d.Z).Unit
                hum:Move(mv,false); hrp.AssemblyLinearVelocity = Vector3.new(mv.X*NS, hrp.AssemblyLinearVelocity.Y, mv.Z*NS); return
            end
            local d = AP_R1 - hrp.Position; local mv = Vector3.new(d.X,0,d.Z).Unit
            hum:Move(mv,false); hrp.AssemblyLinearVelocity = Vector3.new(mv.X*NS, hrp.AssemblyLinearVelocity.Y, mv.Z*NS)
        elseif arPhase == 2 then
            local tgt = Vector3.new(AP_R2.X, hrp.Position.Y, AP_R2.Z)
            if (tgt - hrp.Position).Magnitude < 1 then
                hum:Move(Vector3.zero,false); hrp.AssemblyLinearVelocity = Vector3.zero
                autoRightEnabled = false
                if arConn then arConn:Disconnect(); arConn = nil end
                arPhase = 1
                if autoRightSetVisual then autoRightSetVisual(false) end
                if (AP_R_FACE - hrp.Position).Magnitude > 0.01 then
                    hrp.CFrame = CFrame.new(hrp.Position, Vector3.new(AP_R_FACE.X, hrp.Position.Y, AP_R_FACE.Z))
                end
                return
            end
            local d = AP_R2 - hrp.Position; local mv = Vector3.new(d.X,0,d.Z).Unit
            hum:Move(mv,false); hrp.AssemblyLinearVelocity = Vector3.new(mv.X*NS, hrp.AssemblyLinearVelocity.Y, mv.Z*NS)
        end
    end)
end

-- Unwalk
local function disableAnimations()
    local char = Player.Character; if not char then return end
    local hum = char:FindFirstChildOfClass("Humanoid"); if not hum then return end
    for _, track in pairs(unwalkAnimations) do pcall(function() track:Stop() end) end
    unwalkAnimations = {}
    local animator = hum:FindFirstChildOfClass("Animator")
    if animator then
        for _, track in pairs(animator:GetPlayingAnimationTracks()) do
            track:Stop(); table.insert(unwalkAnimations, track)
        end
    end
end

RunService.Heartbeat:Connect(function()
    if not unwalkEnabled then return end
    disableAnimations()
end)

-- TP Floor
local function runTPFloor()
    pcall(function()
        local char = Player.Character; if not char then return end
        local hrp = char:FindFirstChild("HumanoidRootPart"); if not hrp then return end
        local rp = RaycastParams.new(); rp.FilterDescendantsInstances = {char}; rp.FilterType = Enum.RaycastFilterType.Exclude
        local rs = workspace:Raycast(hrp.Position, Vector3.new(0,-500,0), rp)
        if rs then
            hrp.CFrame = CFrame.new(hrp.Position.X, rs.Position.Y + hrp.Size.Y/2 + 0.1, hrp.Position.Z)
            hrp.AssemblyLinearVelocity = Vector3.zero
        end
    end)
end

-- Drop Brainrot
local DAD, DAS, dropActive = 0.2, 150, false
local function runDrop()
    if dropActive then return end
    local char = Player.Character; if not char then return end
    local hrp = char:FindFirstChild("HumanoidRootPart"); if not hrp then return end
    dropActive = true; local t0 = tick(); local conn
    conn = RunService.Heartbeat:Connect(function()
        local r = char and char:FindFirstChild("HumanoidRootPart")
        if not r then conn:Disconnect(); dropActive = false; return end
        if tick()-t0 >= DAD then
            conn:Disconnect()
            local rp = RaycastParams.new(); rp.FilterDescendantsInstances={char}; rp.FilterType=Enum.RaycastFilterType.Exclude
            local rr = workspace:Raycast(r.Position, Vector3.new(0,-2000,0), rp)
            if rr then
                local hum2 = char:FindFirstChildOfClass("Humanoid")
                local off = (hum2 and hum2.HipHeight or 2) + (r.Size.Y/2)
                r.CFrame = CFrame.new(r.Position.X, rr.Position.Y+off, r.Position.Z); r.AssemblyLinearVelocity=Vector3.zero
            end
            dropActive = false; return
        end
        r.AssemblyLinearVelocity = Vector3.new(r.AssemblyLinearVelocity.X, DAS, r.AssemblyLinearVelocity.Z)
    end)
end

-- Medusa Counter
local function findMedusa()
    local char = Player.Character; if not char then return nil end
    for _, t in ipairs(char:GetChildren()) do if t:IsA("Tool") and t.Name:lower():find("medusa") then return t end end
    local bp = Player:FindFirstChild("Backpack")
    if bp then for _, t in ipairs(bp:GetChildren()) do if t:IsA("Tool") and t.Name:lower():find("medusa") then return t end end end
end

local function useMedusa()
    if medusaDebounce or tick()-medusaLastUsed < MEDUSA_COOLDOWN then return end
    local char = Player.Character; if not char then return end
    medusaDebounce = true
    local med = findMedusa()
    if med then
        if med.Parent ~= char then local h = char:FindFirstChildOfClass("Humanoid"); if h then h:EquipTool(med) end end
        pcall(function() med:Activate() end); medusaLastUsed = tick()
    end
    medusaDebounce = false
end

local function onAnchorChanged(part)
    return part:GetPropertyChangedSignal("Anchored"):Connect(function()
        if part.Anchored and part.Transparency == 1 and medusaCounterEnabled then useMedusa() end
    end)
end

local function setupMedusa(char)
    for _, c in pairs(medusaConns) do pcall(function() c:Disconnect() end) end; medusaConns = {}
    if not char then return end
    for _, part in ipairs(char:GetDescendants()) do
        if part:IsA("BasePart") then table.insert(medusaConns, onAnchorChanged(part)) end
    end
    table.insert(medusaConns, char.DescendantAdded:Connect(function(part)
        if part:IsA("BasePart") then table.insert(medusaConns, onAnchorChanged(part)) end
    end))
end

Player.CharacterAdded:Connect(function(char)
    if medusaCounterEnabled then setupMedusa(char) end
    unwalkAnimations = {}
    if unwalkEnabled then task.wait(0.5); disableAnimations() end
end)

-- Keybind listener
UserInputService.InputBegan:Connect(function(input, gpe)
    if gpe then return end
    local kc = input.KeyCode
    if kc == KB.AutoLeft.kb then
        autoLeftEnabled = not autoLeftEnabled
        if autoLeftEnabled then
            if autoRightEnabled then autoRightEnabled = false; if autoRightSetVisual then autoRightSetVisual(false) end; stopAutoRight() end
            startAutoLeft()
        else stopAutoLeft() end
        if autoLeftSetVisual then autoLeftSetVisual(autoLeftEnabled) end
    elseif kc == KB.AutoRight.kb then
        autoRightEnabled = not autoRightEnabled
        if autoRightEnabled then
            if autoLeftEnabled then autoLeftEnabled = false; if autoLeftSetVisual then autoLeftSetVisual(false) end; stopAutoLeft() end
            startAutoRight()
        else stopAutoRight() end
        if autoRightSetVisual then autoRightSetVisual(autoRightEnabled) end
    elseif kc == KB.DropBrainrot.kb then runDrop()
    elseif kc == KB.TPFloor.kb then runTPFloor()
    end
end)

-- ══════════════════════════════════════════════════════════════════════════
-- GUI
-- ══════════════════════════════════════════════════════════════════════════
local sg = Instance.new("ScreenGui")
sg.Name = "CLOVES_HUB"
sg.ResetOnSpawn = false
sg.Parent = Player.PlayerGui

-- ══════════════════════════════════════════════════════════════════════════
-- TOGGLE GUI BUTTON (58x58, top-right corner)
-- ══════════════════════════════════════════════════════════════════════════
local toggleBtn = Instance.new("Frame", sg)
toggleBtn.Size = UDim2.new(0, 58, 0, 58)
toggleBtn.Position = UDim2.new(1, -70, 0, 12)
toggleBtn.BackgroundColor3 = Color3.fromRGB(5, 5, 12)
toggleBtn.BorderSizePixel = 0
toggleBtn.ZIndex = 100
Instance.new("UICorner", toggleBtn).CornerRadius = UDim.new(0, 14)

local toggleStroke = Instance.new("UIStroke", toggleBtn)
toggleStroke.Thickness = 2
local toggleStrokeGrad = Instance.new("UIGradient", toggleStroke)
toggleStrokeGrad.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0,   Color3.fromRGB(0,80,255)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0,0,0)),
    ColorSequenceKeypoint.new(1,   Color3.fromRGB(0,80,255)),
})
task.spawn(function()
    local r = 0
    while toggleBtn.Parent do
        r = (r+3)%360; toggleStrokeGrad.Rotation = r; task.wait(0.02)
    end
end)

local toggleIcon = Instance.new("TextLabel", toggleBtn)
toggleIcon.Size = UDim2.new(1, 0, 0.5, 0)
toggleIcon.Position = UDim2.new(0, 0, 0, 4)
toggleIcon.BackgroundTransparency = 1
toggleIcon.Text = "☰"
toggleIcon.TextColor3 = Color3.fromRGB(0, 80, 255)
toggleIcon.Font = Enum.Font.GothamBold
toggleIcon.TextSize = 22
toggleIcon.ZIndex = 101

local toggleLbl = Instance.new("TextLabel", toggleBtn)
toggleLbl.Size = UDim2.new(1, 0, 0, 14)
toggleLbl.Position = UDim2.new(0, 0, 1, -18)
toggleLbl.BackgroundTransparency = 1
toggleLbl.Text = "HUB"
toggleLbl.TextColor3 = Color3.fromRGB(100, 160, 255)
toggleLbl.Font = Enum.Font.GothamBold
toggleLbl.TextSize = 10
toggleLbl.ZIndex = 101

local toggleClickBtn = Instance.new("TextButton", toggleBtn)
toggleClickBtn.Size = UDim2.new(1, 0, 1, 0)
toggleClickBtn.BackgroundTransparency = 1
toggleClickBtn.Text = ""
toggleClickBtn.ZIndex = 102

-- Main Window
local main = Instance.new("Frame", sg)
main.Name = "Main"
main.Size = UDim2.new(0, 680 * guiScale, 0, 420 * guiScale)
main.Position = UDim2.new(0.5, -340*guiScale, 0.5, -210*guiScale)
main.BackgroundColor3 = C.bg
main.BorderSizePixel = 0
main.Active = true
main.Draggable = true
main.ClipsDescendants = true
Instance.new("UICorner", main).CornerRadius = UDim.new(0, 18*guiScale)

local mainStroke = Instance.new("UIStroke", main)
mainStroke.Thickness = 2
local strokeGrad = Instance.new("UIGradient", mainStroke)
strokeGrad.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0,   Color3.fromRGB(0,80,255)),
    ColorSequenceKeypoint.new(0.2, Color3.fromRGB(0,0,0)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0,80,255)),
    ColorSequenceKeypoint.new(0.8, Color3.fromRGB(0,0,0)),
    ColorSequenceKeypoint.new(1,   Color3.fromRGB(0,80,255)),
})
task.spawn(function()
    local r = 0
    while main.Parent do
        r = (r+3)%360; strokeGrad.Rotation = r; task.wait(0.02)
    end
end)

-- Toggle logic
local guiVisible = true
local function setMainVisible(v)
    guiVisible = v
    main.Visible = v
    toggleIcon.TextColor3 = v and Color3.fromRGB(0, 80, 255) or Color3.fromRGB(100, 160, 255)
    toggleBtn.BackgroundColor3 = v and Color3.fromRGB(5, 5, 12) or Color3.fromRGB(0, 30, 80)
end

toggleClickBtn.MouseButton1Click:Connect(function()
    setMainVisible(not guiVisible)
end)

UserInputService.InputBegan:Connect(function(input, gpe)
    if gpe then return end
    if input.KeyCode == Enum.KeyCode.U then
        setMainVisible(not guiVisible)
    end
end)

-- ── Sidebar ────────────────────────────────────────────────────────────────
local sidebar = Instance.new("Frame", main)
sidebar.Size = UDim2.new(0, 110*guiScale, 1, 0)
sidebar.BackgroundColor3 = Color3.fromRGB(5,5,12)
sidebar.BorderSizePixel = 0
sidebar.ZIndex = 3
Instance.new("UICorner", sidebar).CornerRadius = UDim.new(0, 18*guiScale)

local sidebarFlush = Instance.new("Frame", sidebar)
sidebarFlush.Size = UDim2.new(0, 18*guiScale, 1, 0)
sidebarFlush.Position = UDim2.new(1,-18*guiScale,0,0)
sidebarFlush.BackgroundColor3 = Color3.fromRGB(5,5,12)
sidebarFlush.BorderSizePixel = 0
sidebarFlush.ZIndex = 3

local divider = Instance.new("Frame", main)
divider.Size = UDim2.new(0,1,1,0)
divider.Position = UDim2.new(0, 110*guiScale, 0, 0)
divider.BackgroundColor3 = Color3.fromRGB(0,50,180)
divider.BorderSizePixel = 0
divider.ZIndex = 4

-- Title
local titleLabel = Instance.new("TextLabel", sidebar)
titleLabel.Size = UDim2.new(1,-8*guiScale,0,52*guiScale)
titleLabel.Position = UDim2.new(0,4*guiScale,0,10*guiScale)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "CLOVES\nHUB"
titleLabel.TextColor3 = Color3.fromRGB(0,80,255)
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextSize = 14*guiScale
titleLabel.TextXAlignment = Enum.TextXAlignment.Center
titleLabel.LineHeight = 1.15
titleLabel.ZIndex = 5

local titleLine = Instance.new("Frame", sidebar)
titleLine.Size = UDim2.new(0.65,0,0,1)
titleLine.Position = UDim2.new(0.175,0,0,65*guiScale)
titleLine.BackgroundColor3 = Color3.fromRGB(0,80,255)
titleLine.BorderSizePixel = 0
titleLine.ZIndex = 5

-- ── Content area ────────────────────────────────────────────────────────────
local contentArea = Instance.new("Frame", main)
contentArea.Size = UDim2.new(1,-(115*guiScale),1,-10*guiScale)
contentArea.Position = UDim2.new(0,115*guiScale,0,5*guiScale)
contentArea.BackgroundTransparency = 1
contentArea.BorderSizePixel = 0
contentArea.ZIndex = 2

-- ── Close button ────────────────────────────────────────────────────────────
local closeBtn = Instance.new("TextButton", main)
closeBtn.Size = UDim2.new(0,30*guiScale,0,30*guiScale)
closeBtn.Position = UDim2.new(1,-38*guiScale,0,8*guiScale)
closeBtn.BackgroundTransparency = 1
closeBtn.Text = "×"
closeBtn.TextColor3 = C.textDim
closeBtn.Font = Enum.Font.GothamBold
closeBtn.TextSize = 22*guiScale
closeBtn.ZIndex = 10
closeBtn.MouseButton1Click:Connect(function() sg:Destroy() end)
closeBtn.MouseEnter:Connect(function() closeBtn.TextColor3 = C.danger end)
closeBtn.MouseLeave:Connect(function() closeBtn.TextColor3 = C.textDim end)

-- ══════════════════════════════════════════════════════════════════════════
-- Tab + Row Helpers
-- ══════════════════════════════════════════════════════════════════════════
local tabs = {"Combat","Protect","Visuals","Settings","Keys"}
local tabButtons = {}
local tabPages   = {}
local activeTab  = nil

for _, tabName in ipairs(tabs) do
    local sf = Instance.new("ScrollingFrame", contentArea)
    sf.Name = tabName.."Page"
    sf.Size = UDim2.new(1,0,1,0)
    sf.BackgroundTransparency = 1
    sf.BorderSizePixel = 0
    sf.ScrollBarThickness = 3
    sf.ScrollBarImageColor3 = Color3.fromRGB(0,60,180)
    sf.AutomaticCanvasSize = Enum.AutomaticSize.Y
    sf.CanvasSize = UDim2.new(0,0,0,0)
    sf.Visible = false
    sf.ZIndex = 3
    local ll = Instance.new("UIListLayout", sf)
    ll.SortOrder = Enum.SortOrder.LayoutOrder
    ll.Padding = UDim.new(0,4)
    local pp = Instance.new("UIPadding", sf)
    pp.PaddingLeft   = UDim.new(0, 8*guiScale)
    pp.PaddingRight  = UDim.new(0, 8*guiScale)
    pp.PaddingTop    = UDim.new(0, 8*guiScale)
    pp.PaddingBottom = UDim.new(0, 8*guiScale)
    tabPages[tabName] = sf
end

local function setActiveTab(name)
    activeTab = name
    for _, t in ipairs(tabs) do
        local btn  = tabButtons[t]
        local page = tabPages[t]
        local isActive = (t == name)
        if btn then
            btn.BackgroundTransparency = isActive and 0.75 or 1
            btn.TextColor3 = isActive and Color3.fromRGB(255,255,255) or C.textDim
            local ind = btn:FindFirstChild("Indicator")
            if ind then ind.BackgroundTransparency = isActive and 0 or 1 end
        end
        if page then page.Visible = isActive end
    end
end

for i, tabName in ipairs(tabs) do
    local btn = Instance.new("TextButton", sidebar)
    btn.Name = tabName.."Tab"
    btn.Size = UDim2.new(1,-14*guiScale,0,34*guiScale)
    btn.Position = UDim2.new(0,7*guiScale,0,(74+(i-1)*40)*guiScale)
    btn.BackgroundColor3 = C.blue
    btn.BackgroundTransparency = 1
    btn.Text = tabName
    btn.TextColor3 = C.textDim
    btn.Font = Enum.Font.GothamSemibold
    btn.TextSize = 12*guiScale
    btn.ZIndex = 6
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0,8*guiScale)
    tabButtons[tabName] = btn

    local indicator = Instance.new("Frame", btn)
    indicator.Name = "Indicator"
    indicator.Size = UDim2.new(0,3,0.55,0)
    indicator.Position = UDim2.new(0,3*guiScale,0.225,0)
    indicator.BackgroundColor3 = C.blue
    indicator.BackgroundTransparency = 1
    indicator.BorderSizePixel = 0
    indicator.ZIndex = 7
    Instance.new("UICorner", indicator).CornerRadius = UDim.new(1,0)

    btn.MouseButton1Click:Connect(function() setActiveTab(tabName) end)
    btn.MouseEnter:Connect(function()
        if activeTab ~= tabName then btn.TextColor3 = Color3.fromRGB(180,210,255) end
    end)
    btn.MouseLeave:Connect(function()
        if activeTab ~= tabName then btn.TextColor3 = C.textDim end
    end)
end

-- ══════════════════════════════════════════════════════════════════════════
-- Widget builders
-- ══════════════════════════════════════════════════════════════════════════
local rowOrder = {}
local function nextOrder(tabName)
    rowOrder[tabName] = (rowOrder[tabName] or 0) + 1
    return rowOrder[tabName]
end

local function mkSection(tabName, text)
    local f = Instance.new("Frame", tabPages[tabName])
    f.Size = UDim2.new(1,0,0,22*guiScale)
    f.BackgroundTransparency = 1
    f.BorderSizePixel = 0
    f.LayoutOrder = nextOrder(tabName)

    local bar = Instance.new("Frame", f)
    bar.Size = UDim2.new(0,3,0,12*guiScale)
    bar.Position = UDim2.new(0,0,0.5,-6*guiScale)
    bar.BackgroundColor3 = C.blue
    bar.BorderSizePixel = 0
    Instance.new("UICorner", bar).CornerRadius = UDim.new(0,2)

    local lbl = Instance.new("TextLabel", f)
    lbl.Size = UDim2.new(1,-8,1,0)
    lbl.Position = UDim2.new(0,8,0,0)
    lbl.BackgroundTransparency = 1
    lbl.Text = text:upper()
    lbl.TextColor3 = C.blue
    lbl.Font = Enum.Font.GothamBlack
    lbl.TextSize = 9*guiScale
    lbl.TextXAlignment = Enum.TextXAlignment.Left
end

local function mkToggle(tabName, label, defaultOn, onToggle)
    local row = Instance.new("Frame", tabPages[tabName])
    row.Size = UDim2.new(1,0,0,36*guiScale)
    row.BackgroundColor3 = C.rowBg
    row.BorderSizePixel = 0
    row.LayoutOrder = nextOrder(tabName)
    Instance.new("UICorner", row).CornerRadius = UDim.new(0,8*guiScale)

    row.MouseEnter:Connect(function()
        TweenService:Create(row,TweenInfo.new(0.1),{BackgroundColor3=C.rowHov}):Play()
    end)
    row.MouseLeave:Connect(function()
        TweenService:Create(row,TweenInfo.new(0.1),{BackgroundColor3=C.rowBg}):Play()
    end)

    local lbl = Instance.new("TextLabel", row)
    lbl.Size = UDim2.new(0.6,0,1,0)
    lbl.Position = UDim2.new(0,10*guiScale,0,0)
    lbl.BackgroundTransparency = 1
    lbl.Text = label
    lbl.TextColor3 = C.text
    lbl.Font = Enum.Font.GothamBold
    lbl.TextSize = 12*guiScale
    lbl.TextXAlignment = Enum.TextXAlignment.Left

    local pillBg = Instance.new("Frame", row)
    pillBg.Size = UDim2.new(0,38*guiScale,0,19*guiScale)
    pillBg.Position = UDim2.new(1,-46*guiScale,0.5,-9.5*guiScale)
    pillBg.BackgroundColor3 = defaultOn and C.onBg or C.offBg
    pillBg.BorderSizePixel = 0
    pillBg.ZIndex = 5
    Instance.new("UICorner", pillBg).CornerRadius = UDim.new(1,0)

    local dot = Instance.new("Frame", pillBg)
    dot.Size = UDim2.new(0,13*guiScale,0,13*guiScale)
    dot.Position = defaultOn and UDim2.new(1,-16*guiScale,0.5,-6.5*guiScale) or UDim2.new(0,3*guiScale,0.5,-6.5*guiScale)
    dot.BackgroundColor3 = Color3.fromRGB(255,255,255)
    dot.BorderSizePixel = 0
    dot.ZIndex = 6
    Instance.new("UICorner", dot).CornerRadius = UDim.new(1,0)

    local isOn = defaultOn or false
    local function setV(on)
        isOn = on
        TweenService:Create(pillBg,TweenInfo.new(0.18,Enum.EasingStyle.Quad),{BackgroundColor3=on and C.onBg or C.offBg}):Play()
        TweenService:Create(dot,TweenInfo.new(0.18,Enum.EasingStyle.Back),{
            Position = on and UDim2.new(1,-16*guiScale,0.5,-6.5*guiScale) or UDim2.new(0,3*guiScale,0.5,-6.5*guiScale)
        }):Play()
    end

    local clk = Instance.new("TextButton", row)
    clk.Size = UDim2.new(1,0,1,0)
    clk.BackgroundTransparency = 1
    clk.Text = ""
    clk.ZIndex = 4
    clk.MouseButton1Click:Connect(function()
        isOn = not isOn; setV(isOn)
        if onToggle then onToggle(isOn) end
    end)

    return setV
end



-- ══════════════════════════════════════════════════════════════════════════
-- SPEED CUSTOMIZER PANEL (floating)
-- ══════════════════════════════════════════════════════════════════════════
local speedGui      = nil
local speedActive   = false
local speedConnection = nil
local speedBoxRef, stealBoxRef = nil, nil
local activateBtnRef = nil

local function startSpeedLoop()
    if speedConnection then return end
    speedConnection = RunService.Heartbeat:Connect(function()
        local char = Player.Character; if not char then return end
        local hrp  = char:FindFirstChild("HumanoidRootPart"); if not hrp then return end
        local hum  = char:FindFirstChildOfClass("Humanoid");  if not hum then return end
        local speedNoStealValue = tonumber(speedBoxRef and speedBoxRef.Text) or 53
        local speedStealValue   = tonumber(stealBoxRef and stealBoxRef.Text) or 29
        local md = hum.MoveDirection
        if md.Magnitude > 0 then
            local isSteal  = hum.WalkSpeed < 25
            local curSpeed = isSteal and speedStealValue or speedNoStealValue
            hrp.AssemblyLinearVelocity = Vector3.new(
                md.X * curSpeed,
                hrp.AssemblyLinearVelocity.Y,
                md.Z * curSpeed
            )
        end
    end)
end

local function stopSpeedLoop()
    if speedConnection then speedConnection:Disconnect(); speedConnection = nil end
end

local function buildSpeedGui()
    if speedGui then return end

    speedGui = Instance.new("ScreenGui")
    speedGui.Name = "NightHub_SpeedCustomizer"
    speedGui.ResetOnSpawn = false
    speedGui.Parent = Player:WaitForChild("PlayerGui")

    local FULL_H = 160
    local MINI_H = 68
    local W = 210
    local isMinimized = false

    local panel = Instance.new("Frame", speedGui)
    panel.Size = UDim2.new(0, W, 0, FULL_H)
    panel.Position = UDim2.new(0, 16, 0.18, 0)
    panel.BackgroundColor3 = Color3.fromRGB(8, 8, 18)
    panel.BackgroundTransparency = 0.08
    panel.Active = true
    panel.Draggable = true
    panel.ClipsDescendants = true
    Instance.new("UICorner", panel).CornerRadius = UDim.new(0, 10)

    local panelStroke = Instance.new("UIStroke", panel)
    panelStroke.Color = Color3.fromRGB(0, 120, 255)
    panelStroke.Thickness = 1.5

    local titleBar = Instance.new("Frame", panel)
    titleBar.Size = UDim2.new(1, 0, 0, 30)
    titleBar.BackgroundTransparency = 1

    local minBtn = Instance.new("TextButton", titleBar)
    minBtn.Size = UDim2.new(0, 20, 0, 20)
    minBtn.Position = UDim2.new(0, 6, 0.5, -10)
    minBtn.BackgroundTransparency = 1
    minBtn.Text = "▲"
    minBtn.TextColor3 = Color3.fromRGB(0, 120, 255)
    minBtn.Font = Enum.Font.GothamBold
    minBtn.TextSize = 14

    local titleLbl = Instance.new("TextLabel", titleBar)
    titleLbl.Size = UDim2.new(1, -34, 1, 0)
    titleLbl.Position = UDim2.new(0, 30, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = "Speed Customizer"
    titleLbl.Font = Enum.Font.GothamBold
    titleLbl.TextSize = 13
    titleLbl.TextColor3 = Color3.fromRGB(220, 220, 220)
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left

    local spDivider = Instance.new("Frame", panel)
    spDivider.Size = UDim2.new(1, -16, 0, 1)
    spDivider.Position = UDim2.new(0, 8, 0, 30)
    spDivider.BackgroundColor3 = Color3.fromRGB(0, 100, 220)
    spDivider.BackgroundTransparency = 0.55

    activateBtnRef = Instance.new("TextButton", panel)
    activateBtnRef.Size = UDim2.new(1, -16, 0, 30)
    activateBtnRef.Position = UDim2.new(0, 8, 0, 34)
    activateBtnRef.BackgroundColor3 = Color3.fromRGB(35, 35, 55)
    activateBtnRef.Text = "OFF"
    activateBtnRef.TextColor3 = Color3.fromRGB(255, 255, 255)
    activateBtnRef.Font = Enum.Font.GothamBold
    activateBtnRef.TextSize = 13
    Instance.new("UICorner", activateBtnRef).CornerRadius = UDim.new(0, 7)

    local function refreshBtn()
        if speedActive then
            activateBtnRef.Text = "ON"
            activateBtnRef.BackgroundColor3 = Color3.fromRGB(0, 120, 255)
        else
            activateBtnRef.Text = "OFF"
            activateBtnRef.BackgroundColor3 = Color3.fromRGB(35, 35, 55)
        end
    end

    activateBtnRef.MouseButton1Click:Connect(function()
        speedActive = not speedActive
        if speedActive then startSpeedLoop() else stopSpeedLoop() end
        refreshBtn()
    end)

    local content = Instance.new("Frame", panel)
    content.Size = UDim2.new(1, 0, 0, FULL_H - 68)
    content.Position = UDim2.new(0, 0, 0, 68)
    content.BackgroundTransparency = 1

    local function makeRow(labelText, defaultVal, yPos)
        local bg = Instance.new("Frame", content)
        bg.Size = UDim2.new(1, -16, 0, 28)
        bg.Position = UDim2.new(0, 8, 0, yPos)
        bg.BackgroundColor3 = Color3.fromRGB(14, 14, 28)
        Instance.new("UICorner", bg).CornerRadius = UDim.new(0, 6)

        local lbl = Instance.new("TextLabel", bg)
        lbl.Size = UDim2.new(0.52, 0, 1, 0)
        lbl.Position = UDim2.new(0, 8, 0, 0)
        lbl.BackgroundTransparency = 1
        lbl.Text = labelText
        lbl.Font = Enum.Font.GothamSemibold
        lbl.TextSize = 12
        lbl.TextColor3 = Color3.fromRGB(200, 200, 200)
        lbl.TextXAlignment = Enum.TextXAlignment.Left

        local box = Instance.new("TextBox", bg)
        box.Size = UDim2.new(0.38, 0, 0.78, 0)
        box.Position = UDim2.new(0.58, 0, 0.11, 0)
        box.BackgroundColor3 = Color3.fromRGB(6, 6, 18)
        box.TextColor3 = Color3.fromRGB(80, 170, 255)
        box.Text = tostring(defaultVal)
        box.Font = Enum.Font.GothamBold
        box.TextSize = 12
        box.ClearTextOnFocus = false
        Instance.new("UICorner", box).CornerRadius = UDim.new(0, 5)

        local s = Instance.new("UIStroke", box)
        s.Color = Color3.fromRGB(0, 100, 220)
        s.Thickness = 1
        s.Transparency = 0.3
        return box
    end

    speedBoxRef = makeRow("Normal Speed", 53, 4)
    stealBoxRef = makeRow("Steal Speed",  29, 38)

    local function sanitise(box, mn, mx, def)
        box.FocusLost:Connect(function()
            local n = tonumber(box.Text)
            if not n then box.Text = tostring(def); return end
            box.Text = tostring(math.clamp(math.floor(n), mn, mx))
        end)
    end
    sanitise(speedBoxRef, 15, 300, 53)
    sanitise(stealBoxRef, 15, 300, 29)

    local function setMinimized(state)
        isMinimized = state
        minBtn.Text = isMinimized and "▼" or "▲"
        TweenService:Create(panel, TweenInfo.new(0.22, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
            {Size = UDim2.new(0, W, 0, isMinimized and MINI_H or FULL_H)}):Play()
        content.Visible = not isMinimized
    end

    minBtn.MouseButton1Click:Connect(function() setMinimized(not isMinimized) end)
end

local function showSpeedGui()
    buildSpeedGui()
    speedGui.Enabled = true
end

local function hideSpeedGui()
    if speedGui then
        speedGui.Enabled = false
        speedActive = false
        stopSpeedLoop()
        if activateBtnRef then
            activateBtnRef.Text = "OFF"
            activateBtnRef.BackgroundColor3 = Color3.fromRGB(35, 35, 55)
        end
    end
end

-- ══════════════════════════════════════════════════════════════════════════
-- INF JUMP
-- ══════════════════════════════════════════════════════════════════════════
local infJumpEnabled  = false
local INF_JUMP_FORCE  = 54
local CLAMP_FALL      = 80

UserInputService.JumpRequest:Connect(function()
    if not infJumpEnabled then return end
    local hrp = Player.Character and Player.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    hrp.AssemblyLinearVelocity = Vector3.new(hrp.AssemblyLinearVelocity.X, INF_JUMP_FORCE, hrp.AssemblyLinearVelocity.Z)
end)

RunService.Heartbeat:Connect(function()
    if not infJumpEnabled then return end
    local hrp = Player.Character and Player.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    if hrp.AssemblyLinearVelocity.Y < -CLAMP_FALL then
        hrp.AssemblyLinearVelocity = Vector3.new(hrp.AssemblyLinearVelocity.X, -CLAMP_FALL, hrp.AssemblyLinearVelocity.Z)
    end
end)

-- ══════════════════════════════════════════════════════════════════════════
-- ANTI RAGDOLL
-- ══════════════════════════════════════════════════════════════════════════
local antiRagdollEnabled = false
local AntiRagdollConns   = {}

local function startAntiRagdoll()
    if #AntiRagdollConns > 0 then return end
    local c = Player.Character or Player.CharacterAdded:Wait()
    local humanoid = c:WaitForChild("Humanoid")
    local root     = c:WaitForChild("HumanoidRootPart")
    local animator = humanoid:WaitForChild("Animator")
    local maxVelocity = 40
    local clampVelocity = 25
    local maxClamp = 15
    local lastVelocity = Vector3.new(0, 0, 0)

    local function IsRagdollState()
        local state = humanoid:GetState()
        return state == Enum.HumanoidStateType.Physics
            or state == Enum.HumanoidStateType.Ragdoll
            or state == Enum.HumanoidStateType.FallingDown
            or state == Enum.HumanoidStateType.GettingUp
    end

    local function CleanRagdollEffects()
        for _, obj in pairs(c:GetDescendants()) do
            if obj:IsA("BallSocketConstraint") or obj:IsA("NoCollisionConstraint")
                or obj:IsA("HingeConstraint")
                or (obj:IsA("Attachment") and (obj.Name == "A" or obj.Name == "B")) then
                obj:Destroy()
            elseif obj:IsA("BodyVelocity") or obj:IsA("BodyPosition") or obj:IsA("BodyGyro") then
                obj:Destroy()
            elseif obj:IsA("Motor6D") then
                obj.Enabled = true
            end
        end
        for _, track in pairs(animator:GetPlayingAnimationTracks()) do
            local n = track.Animation and track.Animation.Name:lower() or ""
            if n:find("rag") or n:find("fall") or n:find("hurt") or n:find("down") then
                track:Stop(0)
            end
        end
    end

    local function ReEnableControls()
        pcall(function()
            require(Player:WaitForChild("PlayerScripts"):WaitForChild("PlayerModule")):GetControls():Enable()
        end)
    end

    table.insert(AntiRagdollConns, humanoid.StateChanged:Connect(function()
        if IsRagdollState() then
            humanoid:ChangeState(Enum.HumanoidStateType.Running)
            CleanRagdollEffects()
            workspace.CurrentCamera.CameraSubject = humanoid
            ReEnableControls()
        end
    end))

    table.insert(AntiRagdollConns, RunService.Heartbeat:Connect(function()
        if not antiRagdollEnabled then return end
        if IsRagdollState() then
            CleanRagdollEffects()
            local vel = root.AssemblyLinearVelocity
            if (vel - lastVelocity).Magnitude > maxVelocity and vel.Magnitude > clampVelocity then
                root.AssemblyLinearVelocity = vel.Unit * math.min(vel.Magnitude, maxClamp)
            end
            lastVelocity = vel
        end
    end))

    table.insert(AntiRagdollConns, c.DescendantAdded:Connect(function()
        if IsRagdollState() then CleanRagdollEffects() end
    end))

    table.insert(AntiRagdollConns, Player.CharacterAdded:Connect(function(newChar)
        c        = newChar
        humanoid = newChar:WaitForChild("Humanoid")
        root     = newChar:WaitForChild("HumanoidRootPart")
        animator = humanoid:WaitForChild("Animator")
        lastVelocity = Vector3.new(0, 0, 0)
        ReEnableControls()
        CleanRagdollEffects()
    end))

    ReEnableControls()
    CleanRagdollEffects()
end

local function stopAntiRagdoll()
    for _, conn in pairs(AntiRagdollConns) do pcall(function() conn:Disconnect() end) end
    AntiRagdollConns = {}
end

-- ══════════════════════════════════════════════════════════════════════════
-- AUTO PLAY PANEL (floating)
-- ══════════════════════════════════════════════════════════════════════════
local autoPlayGui = nil
local aplOn = false
local aprOn = false
local aplPhase = 1
local aprPhase = 1
local aplConn = nil
local aprConn = nil
local selectedAutoSide = "Left"

local POS_L1 = Vector3.new(-476.48, -6.28, 92.73)
local POS_L2 = Vector3.new(-483.12, -4.95, 94.80)
local POS_R1 = Vector3.new(-476.16, -6.52, 25.62)
local POS_R2 = Vector3.new(-483.04, -5.09, 23.14)
local LFINAL = Vector3.new(-473.38, -8.40, 22.34)
local RFINAL = Vector3.new(-476.17, -7.91, 97.91)

local function getHRP()
    local c = Player.Character
    return c and c:FindFirstChild("HumanoidRootPart")
end

local function getHum()
    local c = Player.Character
    return c and c:FindFirstChildOfClass("Humanoid")
end

local autoPlaySpeedBoxRef   = nil
local autoReturnSpeedBoxRef = nil
local autoPlayBtnRef        = nil
local autoSideLeftBtnRef    = nil
local autoSideRightBtnRef   = nil

local function stopAutoPlayLeft()
    aplOn = false
    if aplConn then aplConn:Disconnect(); aplConn = nil end
    aplPhase = 1
    local hh = getHum()
    if hh then hh:Move(Vector3.zero, false) end
end

local function stopAutoPlayRight()
    aprOn = false
    if aprConn then aprConn:Disconnect(); aprConn = nil end
    aprPhase = 1
    local hh = getHum()
    if hh then hh:Move(Vector3.zero, false) end
end

local function startAutoPlayLeft()
    if aplConn then aplConn:Disconnect() end
    aplPhase = 1
    aplConn = RunService.Heartbeat:Connect(function()
        if not aplOn then return end
        local gHrp = getHRP()
        local gHum = getHum()
        if not gHrp or not gHum then return end
        local autoPlaySpeed   = tonumber(autoPlaySpeedBoxRef   and autoPlaySpeedBoxRef.Text)   or 60
        local autoReturnSpeed = tonumber(autoReturnSpeedBoxRef and autoReturnSpeedBoxRef.Text) or 29
        if aplPhase == 1 then
            local d = Vector3.new(POS_L1.X - gHrp.Position.X, 0, POS_L1.Z - gHrp.Position.Z)
            if d.Magnitude < 1 then aplPhase = 2; return end
            local md = d.Unit
            gHum:Move(md, false)
            gHrp.AssemblyLinearVelocity = Vector3.new(md.X * autoPlaySpeed, gHrp.AssemblyLinearVelocity.Y, md.Z * autoPlaySpeed)
        elseif aplPhase == 2 then
            local d = Vector3.new(POS_L2.X - gHrp.Position.X, 0, POS_L2.Z - gHrp.Position.Z)
            if d.Magnitude < 1 then
                aplPhase = 0
                gHum:Move(Vector3.zero, false)
                gHrp.AssemblyLinearVelocity = Vector3.zero
                task.delay(0.1, function() if aplOn then aplPhase = 3 end end)
                return
            end
            local md = d.Unit
            gHum:Move(md, false)
            gHrp.AssemblyLinearVelocity = Vector3.new(md.X * autoPlaySpeed, gHrp.AssemblyLinearVelocity.Y, md.Z * autoPlaySpeed)
        elseif aplPhase == 3 then
            local d = Vector3.new(POS_L1.X - gHrp.Position.X, 0, POS_L1.Z - gHrp.Position.Z)
            if d.Magnitude < 1 then aplPhase = 4; return end
            local md = d.Unit
            gHum:Move(md, false)
            gHrp.AssemblyLinearVelocity = Vector3.new(md.X * autoReturnSpeed, gHrp.AssemblyLinearVelocity.Y, md.Z * autoReturnSpeed)
        elseif aplPhase == 4 then
            local d = Vector3.new(LFINAL.X - gHrp.Position.X, 0, LFINAL.Z - gHrp.Position.Z)
            if d.Magnitude < 1 then
                gHum:Move(Vector3.zero, false)
                gHrp.AssemblyLinearVelocity = Vector3.zero
                stopAutoPlayLeft()
                if autoPlayBtnRef then autoPlayBtnRef.Text = "▶ PLAY"; autoPlayBtnRef.BackgroundColor3 = Color3.fromRGB(0, 120, 255) end
                return
            end
            local md = d.Unit
            gHum:Move(md, false)
            gHrp.AssemblyLinearVelocity = Vector3.new(md.X * autoReturnSpeed, gHrp.AssemblyLinearVelocity.Y, md.Z * autoReturnSpeed)
        end
    end)
end

local function startAutoPlayRight()
    if aprConn then aprConn:Disconnect() end
    aprPhase = 1
    aprConn = RunService.Heartbeat:Connect(function()
        if not aprOn then return end
        local gHrp = getHRP()
        local gHum = getHum()
        if not gHrp or not gHum then return end
        local autoPlaySpeed   = tonumber(autoPlaySpeedBoxRef   and autoPlaySpeedBoxRef.Text)   or 60
        local autoReturnSpeed = tonumber(autoReturnSpeedBoxRef and autoReturnSpeedBoxRef.Text) or 29
        if aprPhase == 1 then
            local d = Vector3.new(POS_R1.X - gHrp.Position.X, 0, POS_R1.Z - gHrp.Position.Z)
            if d.Magnitude < 1 then aprPhase = 2; return end
            local md = d.Unit
            gHum:Move(md, false)
            gHrp.AssemblyLinearVelocity = Vector3.new(md.X * autoPlaySpeed, gHrp.AssemblyLinearVelocity.Y, md.Z * autoPlaySpeed)
        elseif aprPhase == 2 then
            local d = Vector3.new(POS_R2.X - gHrp.Position.X, 0, POS_R2.Z - gHrp.Position.Z)
            if d.Magnitude < 1 then
                aprPhase = 0
                gHum:Move(Vector3.zero, false)
                gHrp.AssemblyLinearVelocity = Vector3.zero
                task.delay(0.1, function() if aprOn then aprPhase = 3 end end)
                return
            end
            local md = d.Unit
            gHum:Move(md, false)
            gHrp.AssemblyLinearVelocity = Vector3.new(md.X * autoPlaySpeed, gHrp.AssemblyLinearVelocity.Y, md.Z * autoPlaySpeed)
        elseif aprPhase == 3 then
            local d = Vector3.new(POS_R1.X - gHrp.Position.X, 0, POS_R1.Z - gHrp.Position.Z)
            if d.Magnitude < 1 then aprPhase = 4; return end
            local md = d.Unit
            gHum:Move(md, false)
            gHrp.AssemblyLinearVelocity = Vector3.new(md.X * autoReturnSpeed, gHrp.AssemblyLinearVelocity.Y, md.Z * autoReturnSpeed)
        elseif aprPhase == 4 then
            local d = Vector3.new(RFINAL.X - gHrp.Position.X, 0, RFINAL.Z - gHrp.Position.Z)
            if d.Magnitude < 1 then
                gHum:Move(Vector3.zero, false)
                gHrp.AssemblyLinearVelocity = Vector3.zero
                stopAutoPlayRight()
                if autoPlayBtnRef then autoPlayBtnRef.Text = "▶ PLAY"; autoPlayBtnRef.BackgroundColor3 = Color3.fromRGB(0, 120, 255) end
                return
            end
            local md = d.Unit
            gHum:Move(md, false)
            gHrp.AssemblyLinearVelocity = Vector3.new(md.X * autoReturnSpeed, gHrp.AssemblyLinearVelocity.Y, md.Z * autoReturnSpeed)
        end
    end)
end

local function isAutoPlaying() return aplOn or aprOn end

local function startAutoPlay()
    if selectedAutoSide == "Left" then
        if aprOn then stopAutoPlayRight() end
        aplOn = true; startAutoPlayLeft()
    else
        if aplOn then stopAutoPlayLeft() end
        aprOn = true; startAutoPlayRight()
    end
end

local function stopAutoPlay()
    if aplOn then stopAutoPlayLeft() end
    if aprOn then stopAutoPlayRight() end
end

local function buildAutoPlayGui()
    if autoPlayGui then return end

    autoPlayGui = Instance.new("ScreenGui")
    autoPlayGui.Name = "NightHub_AutoPlay"
    autoPlayGui.ResetOnSpawn = false
    autoPlayGui.Parent = Player:WaitForChild("PlayerGui")

    local FULL_H = 220
    local MINI_H = 68
    local W = 230
    local isMinimized = false

    local panel = Instance.new("Frame", autoPlayGui)
    panel.Size = UDim2.new(0, W, 0, FULL_H)
    panel.Position = UDim2.new(0, 240, 0.18, 0)
    panel.BackgroundColor3 = Color3.fromRGB(8, 8, 18)
    panel.BackgroundTransparency = 0.08
    panel.Active = true
    panel.Draggable = true
    panel.ClipsDescendants = true
    Instance.new("UICorner", panel).CornerRadius = UDim.new(0, 10)

    local panelStroke = Instance.new("UIStroke", panel)
    panelStroke.Color = Color3.fromRGB(0, 120, 255)
    panelStroke.Thickness = 1.5

    local titleBar = Instance.new("Frame", panel)
    titleBar.Size = UDim2.new(1, 0, 0, 30)
    titleBar.BackgroundTransparency = 1

    local minBtn = Instance.new("TextButton", titleBar)
    minBtn.Size = UDim2.new(0, 20, 0, 20)
    minBtn.Position = UDim2.new(0, 6, 0.5, -10)
    minBtn.BackgroundTransparency = 1
    minBtn.Text = "▲"
    minBtn.TextColor3 = Color3.fromRGB(0, 120, 255)
    minBtn.Font = Enum.Font.GothamBold
    minBtn.TextSize = 14

    local titleLbl = Instance.new("TextLabel", titleBar)
    titleLbl.Size = UDim2.new(1, -34, 1, 0)
    titleLbl.Position = UDim2.new(0, 30, 0, 0)
    titleLbl.BackgroundTransparency = 1
    titleLbl.Text = "Auto Play"
    titleLbl.Font = Enum.Font.GothamBold
    titleLbl.TextSize = 13
    titleLbl.TextColor3 = Color3.fromRGB(220, 220, 220)
    titleLbl.TextXAlignment = Enum.TextXAlignment.Left

    local apDivider = Instance.new("Frame", panel)
    apDivider.Size = UDim2.new(1, -16, 0, 1)
    apDivider.Position = UDim2.new(0, 8, 0, 30)
    apDivider.BackgroundColor3 = Color3.fromRGB(0, 100, 220)
    apDivider.BackgroundTransparency = 0.55

    autoPlayBtnRef = Instance.new("TextButton", panel)
    autoPlayBtnRef.Size = UDim2.new(1, -16, 0, 30)
    autoPlayBtnRef.Position = UDim2.new(0, 8, 0, 34)
    autoPlayBtnRef.BackgroundColor3 = Color3.fromRGB(0, 100, 220)
    autoPlayBtnRef.Text = "▶ PLAY"
    autoPlayBtnRef.TextColor3 = Color3.fromRGB(255, 255, 255)
    autoPlayBtnRef.Font = Enum.Font.GothamBold
    autoPlayBtnRef.TextSize = 13
    Instance.new("UICorner", autoPlayBtnRef).CornerRadius = UDim.new(0, 7)

    autoPlayBtnRef.MouseButton1Click:Connect(function()
        if isAutoPlaying() then
            stopAutoPlay()
        else
            startAutoPlay()
        end
        if isAutoPlaying() then
            autoPlayBtnRef.Text = "⏹ STOP"
            autoPlayBtnRef.BackgroundColor3 = Color3.fromRGB(180, 40, 40)
        else
            autoPlayBtnRef.Text = "▶ PLAY"
            autoPlayBtnRef.BackgroundColor3 = Color3.fromRGB(0, 120, 255)
        end
    end)

    local content = Instance.new("Frame", panel)
    content.Size = UDim2.new(1, 0, 0, FULL_H - 68)
    content.Position = UDim2.new(0, 0, 0, 68)
    content.BackgroundTransparency = 1

    local sideLabel = Instance.new("TextLabel", content)
    sideLabel.Size = UDim2.new(1, -16, 0, 18)
    sideLabel.Position = UDim2.new(0, 8, 0, 4)
    sideLabel.BackgroundTransparency = 1
    sideLabel.Text = "Side"
    sideLabel.Font = Enum.Font.GothamSemibold
    sideLabel.TextSize = 11
    sideLabel.TextColor3 = Color3.fromRGB(100, 160, 255)
    sideLabel.TextXAlignment = Enum.TextXAlignment.Left

    local sideRow = Instance.new("Frame", content)
    sideRow.Size = UDim2.new(1, -16, 0, 30)
    sideRow.Position = UDim2.new(0, 8, 0, 24)
    sideRow.BackgroundColor3 = Color3.fromRGB(14, 14, 28)
    Instance.new("UICorner", sideRow).CornerRadius = UDim.new(0, 6)

    autoSideLeftBtnRef = Instance.new("TextButton", sideRow)
    autoSideLeftBtnRef.Size = UDim2.new(0.5, -2, 1, 0)
    autoSideLeftBtnRef.Position = UDim2.new(0, 0, 0, 0)
    autoSideLeftBtnRef.BackgroundColor3 = Color3.fromRGB(0, 100, 220)
    autoSideLeftBtnRef.Text = "Left"
    autoSideLeftBtnRef.TextColor3 = Color3.fromRGB(255, 255, 255)
    autoSideLeftBtnRef.Font = Enum.Font.GothamBold
    autoSideLeftBtnRef.TextSize = 12
    Instance.new("UICorner", autoSideLeftBtnRef).CornerRadius = UDim.new(0, 6)

    autoSideRightBtnRef = Instance.new("TextButton", sideRow)
    autoSideRightBtnRef.Size = UDim2.new(0.5, -2, 1, 0)
    autoSideRightBtnRef.Position = UDim2.new(0.5, 2, 0, 0)
    autoSideRightBtnRef.BackgroundColor3 = Color3.fromRGB(14, 14, 28)
    autoSideRightBtnRef.Text = "Right"
    autoSideRightBtnRef.TextColor3 = Color3.fromRGB(100, 160, 255)
    autoSideRightBtnRef.Font = Enum.Font.GothamBold
    autoSideRightBtnRef.TextSize = 12
    Instance.new("UICorner", autoSideRightBtnRef).CornerRadius = UDim.new(0, 6)

    autoSideLeftBtnRef.MouseButton1Click:Connect(function()
        selectedAutoSide = "Left"
        autoSideLeftBtnRef.BackgroundColor3  = Color3.fromRGB(0, 100, 220)
        autoSideLeftBtnRef.TextColor3        = Color3.fromRGB(255, 255, 255)
        autoSideRightBtnRef.BackgroundColor3 = Color3.fromRGB(14, 14, 28)
        autoSideRightBtnRef.TextColor3       = Color3.fromRGB(100, 160, 255)
    end)

    autoSideRightBtnRef.MouseButton1Click:Connect(function()
        selectedAutoSide = "Right"
        autoSideRightBtnRef.BackgroundColor3 = Color3.fromRGB(0, 100, 220)
        autoSideRightBtnRef.TextColor3       = Color3.fromRGB(255, 255, 255)
        autoSideLeftBtnRef.BackgroundColor3  = Color3.fromRGB(14, 14, 28)
        autoSideLeftBtnRef.TextColor3        = Color3.fromRGB(100, 160, 255)
    end)

    local speedsLabel = Instance.new("TextLabel", content)
    speedsLabel.Size = UDim2.new(1, -16, 0, 18)
    speedsLabel.Position = UDim2.new(0, 8, 0, 60)
    speedsLabel.BackgroundTransparency = 1
    speedsLabel.Text = "Speed"
    speedsLabel.Font = Enum.Font.GothamSemibold
    speedsLabel.TextSize = 11
    speedsLabel.TextColor3 = Color3.fromRGB(100, 160, 255)
    speedsLabel.TextXAlignment = Enum.TextXAlignment.Left

    local function makeSpeedRow(labelText, defaultVal, yPos)
        local bg = Instance.new("Frame", content)
        bg.Size = UDim2.new(1, -16, 0, 28)
        bg.Position = UDim2.new(0, 8, 0, yPos)
        bg.BackgroundColor3 = Color3.fromRGB(14, 14, 28)
        Instance.new("UICorner", bg).CornerRadius = UDim.new(0, 6)
        local lbl = Instance.new("TextLabel", bg)
        lbl.Size = UDim2.new(0.55, 0, 1, 0)
        lbl.Position = UDim2.new(0, 8, 0, 0)
        lbl.BackgroundTransparency = 1
        lbl.Text = labelText
        lbl.Font = Enum.Font.GothamSemibold
        lbl.TextSize = 11
        lbl.TextColor3 = Color3.fromRGB(200, 200, 200)
        lbl.TextXAlignment = Enum.TextXAlignment.Left
        local box = Instance.new("TextBox", bg)
        box.Size = UDim2.new(0.38, 0, 0.78, 0)
        box.Position = UDim2.new(0.58, 0, 0.11, 0)
        box.BackgroundColor3 = Color3.fromRGB(6, 6, 18)
        box.TextColor3 = Color3.fromRGB(80, 170, 255)
        box.Text = tostring(defaultVal)
        box.Font = Enum.Font.GothamBold
        box.TextSize = 12
        box.ClearTextOnFocus = false
        Instance.new("UICorner", box).CornerRadius = UDim.new(0, 5)
        local s = Instance.new("UIStroke", box)
        s.Color = Color3.fromRGB(0, 100, 220)
        s.Thickness = 1
        s.Transparency = 0.3
        return box
    end

    autoPlaySpeedBoxRef   = makeSpeedRow("Play Speed",   60, 80)
    autoReturnSpeedBoxRef = makeSpeedRow("Return Speed", 29, 114)

    local function sanitise(box, mn, mx, def)
        box.FocusLost:Connect(function()
            local n = tonumber(box.Text)
            if not n then box.Text = tostring(def); return end
            box.Text = tostring(math.clamp(math.floor(n), mn, mx))
        end)
    end
    sanitise(autoPlaySpeedBoxRef,   10, 300, 60)
    sanitise(autoReturnSpeedBoxRef, 10, 300, 29)

    local function setMinimized(state)
        isMinimized = state
        minBtn.Text = isMinimized and "▼" or "▲"
        TweenService:Create(panel, TweenInfo.new(0.22, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
            {Size = UDim2.new(0, W, 0, isMinimized and MINI_H or FULL_H)}):Play()
        content.Visible = not isMinimized
    end

    minBtn.MouseButton1Click:Connect(function() setMinimized(not isMinimized) end)
end

local function showAutoPlayGui()
    buildAutoPlayGui()
    autoPlayGui.Enabled = true
end

local function hideAutoPlayGui()
    if autoPlayGui then
        autoPlayGui.Enabled = false
        stopAutoPlay()
    end
end

-- ══════════════════════════════════════════════════════════════════════════
-- QUICK ACTION BUTTONS (TP Down, Drop, Auto Left, Auto Right)
-- ══════════════════════════════════════════════════════════════════════════
local tpDownGui = nil
local tpDownBtnRef = nil

local function buildTPDownButton()
    if tpDownGui then return end
    tpDownGui = Instance.new("ScreenGui")
    tpDownGui.Name = "NightHub_TPDown"
    tpDownGui.ResetOnSpawn = false
    tpDownGui.Parent = Player:WaitForChild("PlayerGui")

    local btn = Instance.new("TextButton", tpDownGui)
    btn.Size = UDim2.new(0, 120, 0, 40)
    btn.Position = UDim2.new(0.5, -130, 0.5, 0)
    btn.BackgroundColor3 = Color3.fromRGB(0, 80, 255)
    btn.BorderSizePixel = 0
    btn.Text = "TP Down"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 13
    btn.Active = true
    btn.Draggable = true
    btn.ZIndex = 5
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 10)

    local stroke = Instance.new("UIStroke", btn)
    stroke.Color = Color3.fromRGB(0, 80, 255)
    stroke.Thickness = 1.5

    btn.MouseButton1Click:Connect(function()
        runTPFloor()
    end)

    tpDownBtnRef = btn
end

local function showTPDownButton()
    buildTPDownButton()
    tpDownGui.Enabled = true
end

local function hideTPDownButton()
    if tpDownGui then tpDownGui.Enabled = false end
end

local dropGui = nil
local dropBtnRef = nil

local function buildDropButton()
    if dropGui then return end
    dropGui = Instance.new("ScreenGui")
    dropGui.Name = "NightHub_Drop"
    dropGui.ResetOnSpawn = false
    dropGui.Parent = Player:WaitForChild("PlayerGui")

    local btn = Instance.new("TextButton", dropGui)
    btn.Size = UDim2.new(0, 120, 0, 40)
    btn.Position = UDim2.new(0.5, 10, 0.5, 0)
    btn.BackgroundColor3 = Color3.fromRGB(0, 80, 255)
    btn.BorderSizePixel = 0
    btn.Text = "Drop"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 13
    btn.Active = true
    btn.Draggable = true
    btn.ZIndex = 5
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 10)

    local stroke = Instance.new("UIStroke", btn)
    stroke.Color = Color3.fromRGB(0, 80, 255)
    stroke.Thickness = 1.5

    btn.MouseButton1Click:Connect(function()
        runDrop()
    end)

    dropBtnRef = btn
end

local function showDropButton()
    buildDropButton()
    dropGui.Enabled = true
end

local function hideDropButton()
    if dropGui then dropGui.Enabled = false end
end

local alGui = nil
local alBtnRef = nil

local function buildAutoLeftButton()
    if alGui then return end
    alGui = Instance.new("ScreenGui")
    alGui.Name = "NightHub_AutoLeft"
    alGui.ResetOnSpawn = false
    alGui.Parent = Player:WaitForChild("PlayerGui")

    local btn = Instance.new("TextButton", alGui)
    btn.Size = UDim2.new(0, 120, 0, 40)
    btn.Position = UDim2.new(0.5, -130, 0.5, 50)
    btn.BackgroundColor3 = autoLeftEnabled and Color3.fromRGB(255, 0, 0) or Color3.fromRGB(0, 80, 255)
    btn.BorderSizePixel = 0
    btn.Text = "Auto Left"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 13
    btn.Active = true
    btn.Draggable = true
    btn.ZIndex = 5
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 10)

    local stroke = Instance.new("UIStroke", btn)
    stroke.Color = autoLeftEnabled and Color3.fromRGB(255, 0, 0) or Color3.fromRGB(0, 80, 255)
    stroke.Thickness = 1.5

    btn.MouseButton1Click:Connect(function()
        autoLeftEnabled = not autoLeftEnabled
        if autoLeftEnabled then
            if autoRightEnabled then
                autoRightEnabled = false
                if autoRightSetVisual then autoRightSetVisual(false) end
                stopAutoRight()
                hideAutoRightButton()
            end
            startAutoLeft()
            TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(255, 0, 0)}):Play()
            TweenService:Create(stroke, TweenInfo.new(0.15), {Color = Color3.fromRGB(255, 0, 0)}):Play()
        else
            stopAutoLeft()
            TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(0, 80, 255)}):Play()
            TweenService:Create(stroke, TweenInfo.new(0.15), {Color = Color3.fromRGB(0, 80, 255)}):Play()
        end
        if autoLeftSetVisual then autoLeftSetVisual(autoLeftEnabled) end
    end)

    alBtnRef = btn
end

local function showAutoLeftButton()
    buildAutoLeftButton()
    alGui.Enabled = true
end

local function hideAutoLeftButton()
    if alGui then alGui.Enabled = false end
end

local arGui = nil
local arBtnRef = nil

local function buildAutoRightButton()
    if arGui then return end
    arGui = Instance.new("ScreenGui")
    arGui.Name = "NightHub_AutoRight"
    arGui.ResetOnSpawn = false
    arGui.Parent = Player:WaitForChild("PlayerGui")

    local btn = Instance.new("TextButton", arGui)
    btn.Size = UDim2.new(0, 120, 0, 40)
    btn.Position = UDim2.new(0.5, 10, 0.5, 50)
    btn.BackgroundColor3 = Color3.fromRGB(5, 5, 14)
    btn.BorderSizePixel = 0
    btn.Text = "Auto Right"
    btn.TextColor3 = Color3.fromRGB(255, 255, 255)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 13
    btn.Active = true
    btn.Draggable = true
    btn.ZIndex = 5
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 10)

    local stroke = Instance.new("UIStroke", btn)
    stroke.Color = Color3.fromRGB(0, 80, 255)
    stroke.Thickness = 1.5
    local grad = Instance.new("UIGradient", stroke)
    grad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0,   Color3.fromRGB(0,80,255)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0,0,0)),
        ColorSequenceKeypoint.new(1,   Color3.fromRGB(0,80,255)),
    })
    task.spawn(function()
        local r = 0
        while btn.Parent do
            r = (r+3)%360; grad.Rotation = r; task.wait(0.02)
        end
    end)

    btn.MouseEnter:Connect(function()
        local isActive = autoRightEnabled
        if not isActive then
            TweenService:Create(btn, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(0, 40, 120)}):Play()
        end
    end)
    btn.MouseLeave:Connect(function()
        local isActive = autoRightEnabled
        TweenService:Create(btn, TweenInfo.new(0.1), {
            BackgroundColor3 = isActive and Color3.fromRGB(0, 80, 255) or Color3.fromRGB(5, 5, 14)
        }):Play()
    end)

    btn.MouseButton1Click:Connect(function()
        autoRightEnabled = not autoRightEnabled
        if autoRightEnabled then
            if autoLeftEnabled then
                autoLeftEnabled = false
                if autoLeftSetVisual then autoLeftSetVisual(false) end
                stopAutoLeft()
                hideAutoLeftButton()
            end
            startAutoRight()
            TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(0,80,255)}):Play()
            btn.Text = "Auto Right ●"
        else
            stopAutoRight()
            TweenService:Create(btn, TweenInfo.new(0.15), {BackgroundColor3 = Color3.fromRGB(5,5,14)}):Play()
            btn.Text = "Auto Right"
        end
        if autoRightSetVisual then autoRightSetVisual(autoRightEnabled) end
    end)

    arBtnRef = btn
end

local function showAutoRightButton()
    buildAutoRightButton()
    arGui.Enabled = true
end

local function hideAutoRightButton()
    if arGui then arGui.Enabled = false end
end

-- ══════════════════════════════════════════════════════════════════════════
-- COMBAT TAB CONTENT
-- (Removed: Anti Ragdoll, Inf Jump — those are now Protect-only)
-- ══════════════════════════════════════════════════════════════════════════
mkSection("Combat","Movement")

autoLeftSetVisual = mkToggle("Combat","Auto Left",false,function(on)
    if on then
        showAutoLeftButton()
    else
        hideAutoLeftButton()
    end
end)

autoRightSetVisual = mkToggle("Combat","Auto Right",false,function(on)
    if on then
        showAutoRightButton()
    else
        hideAutoRightButton()
    end
end)

mkSection("Combat","Teleport")

mkToggle("Combat","TP Down",false,function(on)
    if on then 
        showTPDownButton()
    else
        hideTPDownButton()
    end
end)

mkToggle("Combat","Drop Brainrot",false,function(on)
    if on then 
        showDropButton()
    else
        hideDropButton()
    end
end)

mkSection("Combat","Utility")

mkToggle("Combat","Unwalk",false,function(on)
    unwalkEnabled = on
    if on then disableAnimations() else unwalkAnimations = {} end
end)

mkToggle("Combat","Medusa Counter",false,function(on)
    medusaCounterEnabled = on
    if on then
        setupMedusa(Player.Character)
    else
        for _, c in pairs(medusaConns) do pcall(function() c:Disconnect() end) end
        medusaConns = {}
    end
end)

mkSection("Combat","Speed Customizer")

mkToggle("Combat","Speed Customizer",false,function(on)
    if on then showSpeedGui() else hideSpeedGui() end
end)

mkSection("Combat","Auto Play")

mkToggle("Combat","Auto Play",false,function(on)
    if on then showAutoPlayGui() else hideAutoPlayGui() end
end)

-- ══════════════════════════════════════════════════════════════════════════
-- VISUALS FEATURE STATE
-- ══════════════════════════════════════════════════════════════════════════
local SpeedBB = nil
local speedLabelBB = nil
local speedVisConn = nil

local function createSpeedBB(char)
    local head = char:FindFirstChild("Head") or char:FindFirstChild("HumanoidRootPart")
    if not head then return end
    if SpeedBB then SpeedBB:Destroy() end
    SpeedBB = Instance.new("BillboardGui")
    SpeedBB.Adornee = head
    SpeedBB.Size = UDim2.new(0, 80, 0, 28)
    SpeedBB.StudsOffset = Vector3.new(0, 3.5, 0)
    SpeedBB.AlwaysOnTop = true
    SpeedBB.Parent = head
    speedLabelBB = Instance.new("TextLabel", SpeedBB)
    speedLabelBB.Size = UDim2.new(1, 0, 1, 0)
    speedLabelBB.BackgroundTransparency = 1
    speedLabelBB.Text = "0"
    speedLabelBB.TextColor3 = Color3.fromRGB(255, 255, 255)
    speedLabelBB.Font = Enum.Font.GothamBold
    speedLabelBB.TextScaled = true
    speedLabelBB.TextStrokeTransparency = 0
    speedLabelBB.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
end

local function startSpeedVis()
    if Player.Character then createSpeedBB(Player.Character) end
    Player.CharacterAdded:Connect(function(char)
        task.wait(0.5)
        createSpeedBB(char)
    end)
    if speedVisConn then speedVisConn:Disconnect() end
    speedVisConn = RunService.Heartbeat:Connect(function()
        local char = Player.Character
        if not char then return end
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if not hrp then return end
        if not SpeedBB or not SpeedBB.Parent then createSpeedBB(char) end
        if speedLabelBB then
            local v = hrp.AssemblyLinearVelocity
            speedLabelBB.Text = tostring(math.floor(Vector3.new(v.X,0,v.Z).Magnitude))
        end
    end)
end

startSpeedVis()

-- Player ESP
local PlayerESP = { Enabled = false, Highlights = {}, Billboards = {}, Connections = {} }

local function createHighlight(character)
    local h = Instance.new("Highlight")
    h.Adornee = character
    h.FillColor = Color3.fromRGB(0, 80, 255)
    h.FillTransparency = 0.3
    h.OutlineColor = Color3.fromRGB(0, 80, 255)
    h.OutlineTransparency = 0
    h.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    h.Parent = Player.PlayerGui
    return h
end

local function createNameBB(character, plr)
    local hrp = character:FindFirstChild("HumanoidRootPart")
    if not hrp then return nil end
    local bb = Instance.new("BillboardGui")
    bb.Adornee = hrp
    bb.AlwaysOnTop = true
    bb.Size = UDim2.new(0, 100, 0, 22)
    bb.StudsOffsetWorldSpace = Vector3.new(0, 3.2, 0)
    bb.MaxDistance = 600
    bb.Parent = Player.PlayerGui
    local bg = Instance.new("Frame", bb)
    bg.Size = UDim2.new(1, 0, 1, 0)
    bg.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    bg.BackgroundTransparency = 0.45
    Instance.new("UICorner", bg).CornerRadius = UDim.new(0, 5)
    local txt = Instance.new("TextLabel", bg)
    txt.Size = UDim2.new(1, 0, 1, 0)
    txt.BackgroundTransparency = 1
    txt.Font = Enum.Font.GothamSemibold
    txt.TextSize = 13
    txt.TextColor3 = Color3.fromRGB(255, 255, 255)
    txt.TextStrokeTransparency = 0.2
    txt.Text = plr.DisplayName
    return bb
end

local function attachESP(plr)
    if plr == Player then return end
    local function apply(char)
        if PlayerESP.Highlights[plr] then pcall(function() PlayerESP.Highlights[plr]:Destroy() end) end
        if PlayerESP.Billboards[plr] then pcall(function() PlayerESP.Billboards[plr]:Destroy() end) end
        PlayerESP.Highlights[plr] = createHighlight(char)
        PlayerESP.Billboards[plr] = createNameBB(char, plr)
    end
    if plr.Character then apply(plr.Character) end
    local conn = plr.CharacterAdded:Connect(apply)
    table.insert(PlayerESP.Connections, conn)
end

local function removeESP(plr)
    if PlayerESP.Highlights[plr] then pcall(function() PlayerESP.Highlights[plr]:Destroy() end) end
    if PlayerESP.Billboards[plr] then pcall(function() PlayerESP.Billboards[plr]:Destroy() end) end
end

local function enableESP()
    if PlayerESP.Enabled then return end
    PlayerESP.Enabled = true
    for _, p in ipairs(Players:GetPlayers()) do attachESP(p) end
    table.insert(PlayerESP.Connections, Players.PlayerAdded:Connect(attachESP))
    table.insert(PlayerESP.Connections, Players.PlayerRemoving:Connect(removeESP))
end

local function disableESP()
    if not PlayerESP.Enabled then return end
    PlayerESP.Enabled = false
    for _, h in pairs(PlayerESP.Highlights) do pcall(function() h:Destroy() end) end
    for _, b in pairs(PlayerESP.Billboards) do pcall(function() b:Destroy() end) end
    for _, c in ipairs(PlayerESP.Connections) do pcall(function() c:Disconnect() end) end
    PlayerESP = { Enabled = false, Highlights = {}, Billboards = {}, Connections = {} }
end

-- Optimizer
local function enableOptimizer()
    workspace.Terrain.WaterWaveSize = 0
    game:GetService("Lighting").GlobalShadows = false
    for _, obj in pairs(game:GetDescendants()) do
        if obj:IsA("BasePart") then obj.Material = Enum.Material.Plastic
        elseif obj:IsA("Decal") then obj.Transparency = 1 end
    end
end

-- Xray Base
local XrayBase = { Enabled = false, OriginalTransp = {}, Conns = {}, Keywords = {"base","claim","plot"} }

local function isPlayerBase(obj)
    if not (obj:IsA("BasePart") or obj:IsA("MeshPart") or obj:IsA("UnionOperation")) then return false end
    local n = obj.Name:lower()
    local p = obj.Parent and obj.Parent.Name:lower() or ""
    for _, kw in ipairs(XrayBase.Keywords) do
        if n:find(kw) or p:find(kw) then return true end
    end
    return false
end

local function applyXray(obj)
    if isPlayerBase(obj) then
        if not XrayBase.OriginalTransp[obj] then XrayBase.OriginalTransp[obj] = obj.LocalTransparencyModifier end
        obj.LocalTransparencyModifier = 0.8
    end
end

local function enableXray()
    if XrayBase.Enabled then return end
    XrayBase.Enabled = true
    for _, obj in ipairs(workspace:GetDescendants()) do applyXray(obj) end
    XrayBase.Conns.added = workspace.DescendantAdded:Connect(applyXray)
    XrayBase.Conns.char = Player.CharacterAdded:Connect(function()
        task.wait(0.5)
        for _, obj in ipairs(workspace:GetDescendants()) do applyXray(obj) end
    end)
end

local function disableXray()
    if not XrayBase.Enabled then return end
    XrayBase.Enabled = false
    for _, c in pairs(XrayBase.Conns) do pcall(function() c:Disconnect() end) end
    XrayBase.Conns = {}
    for obj, orig in pairs(XrayBase.OriginalTransp) do pcall(function() obj.LocalTransparencyModifier = orig end) end
    XrayBase.OriginalTransp = {}
end

-- ══════════════════════════════════════════════════════════════════════════
-- VISUALS TAB CONTENT
-- ══════════════════════════════════════════════════════════════════════════
mkSection("Visuals","Players")
mkToggle("Visuals","Player ESP",false,function(on)
    if on then enableESP() else disableESP() end
end)

mkSection("Visuals","Performance")
mkToggle("Visuals","Optimizer",false,function(on)
    if on then enableOptimizer() end
end)

mkSection("Visuals","World")
mkToggle("Visuals","Xray Base",false,function(on)
    if on then enableXray() else disableXray() end
end)

do
    local row = Instance.new("Frame", tabPages["Visuals"])
    row.Size = UDim2.new(1,0,0,36*guiScale)
    row.BackgroundColor3 = C.rowBg
    row.BorderSizePixel = 0
    row.LayoutOrder = nextOrder("Visuals")
    Instance.new("UICorner", row).CornerRadius = UDim.new(0,8*guiScale)
    local lbl = Instance.new("TextLabel", row)
    lbl.Size = UDim2.new(0.7,0,1,0)
    lbl.Position = UDim2.new(0,10*guiScale,0,0)
    lbl.BackgroundTransparency = 1
    lbl.Text = "Speed Visual"
    lbl.TextColor3 = C.text
    lbl.Font = Enum.Font.GothamBold
    lbl.TextSize = 12*guiScale
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    local badge = Instance.new("TextLabel", row)
    badge.Size = UDim2.new(0,50*guiScale,0,20*guiScale)
    badge.Position = UDim2.new(1,-58*guiScale,0.5,-10*guiScale)
    badge.BackgroundColor3 = C.onBg
    badge.BorderSizePixel = 0
    badge.Text = "ON"
    badge.TextColor3 = Color3.fromRGB(255,255,255)
    badge.Font = Enum.Font.GothamBold
    badge.TextSize = 11*guiScale
    Instance.new("UICorner", badge).CornerRadius = UDim.new(0,6*guiScale)
end

-- ══════════════════════════════════════════════════════════════════════════
-- PROTECT TAB CONTENT
-- Added: Optimizer
-- Kept: Anti Ragdoll, Inf Jump
-- ══════════════════════════════════════════════════════════════════════════
mkSection("Protect","Defense")

mkToggle("Protect","Anti Ragdoll",false,function(on)
    antiRagdollEnabled = on
    if on then startAntiRagdoll() else stopAntiRagdoll() end
end)

mkToggle("Protect","Inf Jump",false,function(on)
    infJumpEnabled = on
end)

mkSection("Protect","Performance")

mkToggle("Protect","Optimizer",false,function(on)
    if on then enableOptimizer() end
end)

-- ══════════════════════════════════════════════════════════════════════════
-- OTHER TABS — placeholder labels
-- ══════════════════════════════════════════════════════════════════════════
for _, tabName in ipairs({"Settings","Keys"}) do
    local ph = Instance.new("TextLabel", tabPages[tabName])
    ph.Size = UDim2.new(1,0,0,28*guiScale)
    ph.BackgroundTransparency = 1
    ph.Text = tabName.." — coming soon"
    ph.TextColor3 = Color3.fromRGB(40,70,140)
    ph.Font = Enum.Font.GothamBold
    ph.TextSize = 12*guiScale
    ph.TextXAlignment = Enum.TextXAlignment.Left
    ph.LayoutOrder = 1
end

-- Default tab
setActiveTab("Combat")